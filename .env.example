#DATABASE VARIABLES
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_NAME=
DB_PORT=5432

# Composed from the above — format: postgres://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=postgres://${DB_USER}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}